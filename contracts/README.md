# Contracts

This folder will hold shared JSON schemas, prompt templates, and interface definitions referenced throughout the AI auditing system (see Sections 4, 13, and 14 of `AI_Auditing_System_Design.md`). Keeping them centralized ensures future agents can evolve the system without breaking existing components.

